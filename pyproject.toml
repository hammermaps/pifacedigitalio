[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pifacedigitalio"
requires-python = ">=3.7"
dependencies = [
    "pifacecommon==4.0.0",
    "paho-mqtt>=1.6.1",
    "PyYAML>=6.0",
]
